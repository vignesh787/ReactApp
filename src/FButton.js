import React from "react";

function FButton() {
  return (
    <div>
      <button onClick={() => console.log("F Button is clicked ")}>
        Function Button
      </button>
    </div>
  );
}

export default FButton;
